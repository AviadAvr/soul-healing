// Real pre-rendered page for the /reiki clean URL (200 instead of the 404
// fallback) so Open Graph previews work when this URL is shared. See about.js.
export { default, getStaticProps } from "./index";

